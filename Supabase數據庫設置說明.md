# 📊 Supabase 數據庫設置說明

## 🎯 功能說明

現在圖片系統已完全遷移到 Supabase：
- ✅ **圖片存儲**：Supabase Storage (`hua-real-estate/invite-photo/`)
- ✅ **圖片 URL 存儲**：Supabase 數據庫表 (`invite_event_images`)
- ✅ **圖片讀取**：直接從 Supabase 數據庫讀取，不再依賴 Google Sheets

## 📋 設置步驟

### 步驟 1：創建數據庫表

1. 打開 Supabase Dashboard：
   - 訪問：https://supabase.com/dashboard/project/sqgrnowrcvspxhuudrqc

2. 進入 SQL Editor：
   - 左側菜單 → `SQL Editor`
   - 點擊 `New query`

3. 執行 SQL 腳本：
   - 打開項目中的 `supabase-invite-images-table.sql` 文件
   - 複製全部內容
   - 貼到 SQL Editor 中
   - 點擊 `Run` 執行

4. 確認表已創建：
   - 左側菜單 → `Table Editor`
   - 應該能看到 `invite_event_images` 表

### 步驟 2：驗證 RLS 策略

表創建後，RLS 策略會自動設置：
- ✅ 允許匿名用戶讀取（SELECT）
- ✅ 允許匿名用戶插入（INSERT）
- ✅ 允許匿名用戶更新（UPDATE）

如果需要檢查：
- 左側菜單 → `Authentication` → `Policies`
- 找到 `invite_event_images` 表
- 應該有 3 個策略：
  - `Allow public read from invite_event_images`
  - `Allow public insert to invite_event_images`
  - `Allow public update invite_event_images`

## 🚀 使用流程

### 上傳圖片

1. 點擊頁面底部的上傳圖標 📤
2. 輸入密碼：`888`
3. 選擇兩張圖片
4. 點擊「上傳圖片」

**系統會自動：**
- ✅ 上傳圖片到 Supabase Storage
- ✅ 保存圖片 URL 到 Supabase 數據庫
- ✅ 自動刷新頁面顯示新圖片

### 查看數據

1. 打開 Supabase Dashboard
2. 進入 `Table Editor`
3. 選擇 `invite_event_images` 表
4. 可以看到所有上傳的圖片記錄

## 📊 數據表結構

| 欄位名稱 | 類型 | 說明 |
|---------|------|------|
| `id` | BIGSERIAL | 主鍵（自動遞增） |
| `event_title` | TEXT | 活動標題（可選） |
| `event_date` | DATE | 活動日期（用於自動選擇） |
| `image1_url` | TEXT | 圖片 1 的 URL |
| `image2_url` | TEXT | 圖片 2 的 URL |
| `created_at` | TIMESTAMPTZ | 創建時間（自動） |
| `updated_at` | TIMESTAMPTZ | 更新時間（自動） |

## 🔄 自動選擇活動邏輯

系統會根據當前日期自動選擇要顯示的活動：

- **週四 09:00 前**：顯示本週的活動
- **週四 09:00 後**：自動切換到下週的活動
- **如果沒有未來的活動**：顯示最新的活動

## ⚠️ 注意事項

### 如果 Supabase 數據庫沒有數據

系統會自動回退到 Google Sheets：
- 如果 `invite_event_images` 表為空
- 或 Supabase 客戶端未初始化
- 系統會自動使用 Google Sheets 作為備用數據源

### 數據遷移（可選）

如果您想將 Google Sheets 中的圖片 URL 遷移到 Supabase：

1. 從 Google Sheets 複製圖片 URL
2. 在 Supabase Dashboard 的 `Table Editor` 中
3. 手動插入記錄，或使用 SQL：

```sql
INSERT INTO invite_event_images (event_title, event_date, image1_url, image2_url)
VALUES ('活動標題', '2024-01-01', '圖片1URL', '圖片2URL');
```

## ✅ 完成後

設置完成後，系統會：
- ✅ 優先從 Supabase 數據庫讀取圖片
- ✅ 上傳圖片時自動保存到數據庫
- ✅ 不再需要更新 Google Sheets
- ✅ 更快的響應速度
- ✅ 統一的數據源

---

## 🆘 故障排除

### 問題：頁面顯示「找不到圖片」

**解決方案：**
1. 檢查 Supabase 數據庫表是否已創建
2. 檢查表中是否有數據
3. 檢查 RLS 策略是否正確設置
4. 查看瀏覽器控制台錯誤信息

### 問題：上傳成功但頁面沒有更新

**解決方案：**
1. 檢查 Supabase 數據庫是否成功插入記錄
2. 檢查瀏覽器控制台是否有錯誤
3. 手動刷新頁面

### 問題：無法上傳圖片

**解決方案：**
1. 檢查 Supabase Storage 策略是否正確（參考 `Supabase存儲策略配置.md`）
2. 檢查 Supabase 客戶端是否正確初始化
3. 檢查 `SUPABASE_CONFIG` 配置是否正確

---

**所有功能已就緒！現在可以完全使用 Supabase 作為數據源了！** 🎉
