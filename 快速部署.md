# ⚡ 快速部署 - 2 步完成

## ✅ 您已經有所有需要的文件了！

- ✅ `r2-upload-worker.js` - Worker 代碼
- ✅ `wrangler.toml` - 配置文件
- ✅ Wrangler 已安裝

## 🚀 只需 2 步：

### 步驟 1：登錄 Cloudflare

在終端運行：
```bash
wrangler login
```

這會打開瀏覽器，登錄您的 Cloudflare 帳戶。

---

### 步驟 2：部署

登錄完成後，運行：
```bash
wrangler deploy
```

部署成功後，您會看到 Worker URL，類似：
```
✨  Success! Published the following Worker:
   https://r2-upload.your-subdomain.workers.dev
```

---

## 📝 最後一步：配置 URL

複製 Worker URL，然後告訴我，我會幫您配置到 `invite.html` 中！

---

## 💡 提示

**不需要使用 `npm create cloudflare`**，因為我們已經有完整的代碼和配置了！

直接使用 `wrangler deploy` 即可。
