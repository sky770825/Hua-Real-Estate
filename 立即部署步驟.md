# 🚀 立即部署步驟 - 3 分鐘完成

## 當前狀態
✅ Wrangler 已安裝
⏳ 需要登錄 Cloudflare

## 步驟 1：登錄 Cloudflare

在終端運行：
```bash
wrangler login
```

這會：
1. 自動打開瀏覽器
2. 要求您登錄 Cloudflare 帳戶
3. 授權 Wrangler 訪問

**完成後回到終端，看到 "Successfully logged in" 即可繼續**

---

## 步驟 2：部署 Worker

登錄完成後，在終端運行：
```bash
cd "/Users/caijunchang/Desktop/程式專案資料夾/Hua-Real-Estate-華地產網站"
wrangler deploy
```

這會：
1. 自動讀取 `wrangler.toml` 配置
2. 部署 `r2-upload-worker.js` 到 Cloudflare
3. 綁定 R2 存儲桶 `hua-real-estate`
4. 顯示 Worker URL

**部署成功後，您會看到類似這樣的輸出：**
```
✨  Compiled Worker successfully
✨  Success! Published the following Worker:
   https://r2-upload.your-subdomain.workers.dev
```

---

## 步驟 3：配置 Worker URL

複製上面顯示的 Worker URL，然後：

1. 打開 `invite.html`
2. 找到第 3097 行（搜索 `apiEndpoint`）
3. 將：
   ```javascript
   apiEndpoint: '', // 當前是空的
   ```
   改為：
   ```javascript
   apiEndpoint: 'https://r2-upload.your-subdomain.workers.dev', // 您的 Worker URL
   ```

---

## 完成！

配置完成後，上傳功能就可以正常使用了！🎉

---

## 如果遇到問題

### 問題：登錄失敗
- 確認您有 Cloudflare 帳戶
- 確認網絡連接正常
- 如果瀏覽器沒有自動打開，手動訪問：https://dash.cloudflare.com

### 問題：部署失敗，提示存儲桶不存在
- 確認存儲桶名稱是 `hua-real-estate`
- 在 Cloudflare Dashboard 確認存儲桶存在
- 確認您有該存儲桶的訪問權限

### 問題：找不到 wrangler.toml
- 確認您在正確的目錄
- 確認 `wrangler.toml` 文件存在

---

## 快速命令總結

```bash
# 1. 登錄（首次使用）
wrangler login

# 2. 部署
wrangler deploy

# 3. 複製 Worker URL，配置到 invite.html
```
